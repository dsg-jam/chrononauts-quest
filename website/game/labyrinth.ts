import { BackendConnection } from "./backend";
import { Terminal } from "@/components/terminal";

export async function labyrinth(
  terminal: Terminal,
  backend: BackendConnection,
): Promise<void> {}
